GuardRails is liek a forewall for Ai--->

<!-- types -->

OPen ai SDK by default porbide these method for guardrails or else you culd check guardrails ai website more over this
and it uses a concept called "tripwire"
1.Input guardRails
2.Output guardRails

<!-- what is the use of guardrails -->

1.It is used to prevent the AI from generating harmful or inappropriate content.
2.It is used to prevent the AI from generating biased or unfair content.
3.It is used to prevent the AI from generating incorrect or misleading content.
4.It is used to prevent the AI from generating content that violates the terms of service.
5.It is used to prevent the AI from generating content that violates the law.
6.It is used to prevent the AI from generating content that violates the privacy of individuals.
7.It is used to prevent the AI from generating content that violates the intellectual property rights of others.
8.It is used to prevent the AI from generating content that violates the rights of others.
9.It is used to prevent the AI from generating content that violates the rights of others.
10.It is used to prevent the AI from generating content that violates the rights of others.
